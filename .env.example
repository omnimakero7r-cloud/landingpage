# Configurações da aplicação Omnimaker
# Copie este arquivo para .env.local e configure as variáveis

# URL base da aplicação (necessário para metadata)
NEXT_PUBLIC_BASE_URL=https://omnimaker.io

# URL base para desenvolvimento
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Configurações de Analytics (opcional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Configurações de verificação de domínio (opcional)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-google-verification-code
NEXT_PUBLIC_YANDEX_VERIFICATION=your-yandex-verification-code

# Configurações de API (se necessário no futuro)
# API_URL=https://api.omnimaker.io
# API_KEY=your-api-key

# Configurações de ambiente
NEXT_PUBLIC_ENVIRONMENT=production
# NEXT_PUBLIC_ENVIRONMENT=development

# Configurações do Service Worker
NEXT_PUBLIC_SW_ENABLED=true

# Configurações de PWA
NEXT_PUBLIC_PWA_ENABLED=true
